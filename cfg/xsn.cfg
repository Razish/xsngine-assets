bind ESCAPE "openMenu settings"
bind SPACE "+jump"
bind ` "toggleconsole"
bind a "+left"
bind c "+crouch"
bind d "+right"
bind q "quit"
bind s "+back"
bind w "+forward"
bind x "reloadMenu"
bind z "reloadTerrain"
bind F1 "cvarlist"
bind F2 "bindlist"
bind F3 "cmdlist"
bind F5 "toggle r_wireframe"
bind F12 "screenshot"
